services:
  audit.redirect_subscriber:
    class: Drupal\audit\EventSubscriber\RedirectSubscriber
    arguments: ["@current_user"]
    tags:
      - { name: event_subscriber }
  
  audit.completion_service:
    class: Drupal\audit\Service\AuditCompletionService
    arguments: ["@entity_type.manager"]
